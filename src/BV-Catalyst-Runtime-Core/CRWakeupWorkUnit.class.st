Class {
	#name : 'CRWakeupWorkUnit',
	#superclass : 'CRWorkUnit',
	#category : 'BV-Catalyst-Runtime-Core',
	#package : 'BV-Catalyst-Runtime-Core'
}

{ #category : 'running' }
CRWakeupWorkUnit >> run [
	"I do nothing. I exist only to wake a worker blocked on its queue."

	
]
